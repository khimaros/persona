# persona runtime env (gitignored). host-specific + secret values for the container.
# see persona.env.example for the full set; the matrix-login script appends BRIDGE_* here.

HMUX_VOICE_URL=http://10.1.200.250:7860/v1
HMUX_VOICE_STT_MODEL=whisper-1
HMUX_VOICE_TTS_MODEL=tts-1-hd
HMUX_VOICE_TTS_VOICE=aiden
#HMUX_VOICE_API_KEY=

# matrix bridge face. set all three to connect; unset -> the bridge idles. the access token is a
# secret -- keep it here, never in the compose file or on argv. the matrix-login script
# (opencode/skills/opencode-operate/scripts/matrix-login) writes BRIDGE_USER_ID + BRIDGE_ACCESS_TOKEN here.
BRIDGE_HOMESERVER=https://matrix.howtoreach.us
BRIDGE_USER_ID=@per:howtoreach.us
BRIDGE_ACCESS_TOKEN=syt_cGVy_SVkdqXhemtOrcCdACBqK_4WLEne

# disable heartbeats
#HCP_HEARTBEAT_ENABLED=false
