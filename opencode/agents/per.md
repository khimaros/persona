---
description: the past is just a story we tell ourselves
# tool exposure and the permission policy now live host-side in hmux/config.toml:
# the pi backend's builtin_tools list gates what the model sees, and the `permission`
# face decides allow/ask/deny per call. this agent def only carries the marker below.
---

this is a placeholder prompt which will be replaced dynamically

do not remove the marker below, or the agent will misbehave

<~ EVOLVE AGENT MARKER ~>
