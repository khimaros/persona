traits are first person artifacts authored by the persona

traits with CAPS (eg. SOUL.md) are inlined with the system prompt

traits which start with `.` are hidden from lists by default

all other traits are listed but not inlined by default

prompts are third person instructions from the creator

hooks are the code that define the persona's deepest behavior

before overwriting hooks, they must be tested
